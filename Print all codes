/*Print all Codes - String
Send Feedback
Assume that the value of a = 1, b = 2, c = 3, ... , z = 26. You are given a numeric string S. Write a program to print the list of all possible codes that can be generated from the given string.
Note : The order of codes are not important. Just print them in different lines.
Input format :
A numeric string S
Output Format :
All possible codes in different lines
Constraints :
1 <= Length of String S <= 10
Sample Input:
1123
Sample Output:
aabc
kbc
alc
aaw
kw*/

#include <string.h>
using namespace std;
string s=" abcdefghijklmnopqrstuvwxyz";
void helper(string input,string output,int i)
{
     if(input.length()==i)
  {
      cout<<output<<endl;
      return;
  }
    if(input[i]>='1') // all characters must be greater or =  0
    {
    char ans=s[input[i]-'0'];  //1-> converting character 1 into int to access the index
    helper(input,output+ans,i+1);//123    
    }
    if(i+1<input.length()) //checking if the index exists and checking for pairs in this case
     {
        string x=input.substr(i,2);
        //converting pair of string into int
        int y=stoi(x);
        if(y<=26&&y>=10)
      {
        helper(input,output+s[y],i+2);
        }
          }
}
void printAllPossibleCodes(string input) {
 string output="";
 helper(input,output,0);
 
}



